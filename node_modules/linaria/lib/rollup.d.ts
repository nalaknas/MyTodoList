/**
 * This file contains a Rollup loader for Linaria.
 * It uses the transform.ts function to generate class names from source code,
 * returns transformed code without template literals and attaches generated source maps
 */
import type { Preprocessor } from './types';
import type { PluginOptions } from './babel/utils/loadOptions';
declare type RollupPluginOptions = {
    include?: string | string[];
    exclude?: string | string[];
    sourceMap?: boolean;
    preprocessor?: Preprocessor;
} & Partial<PluginOptions>;
export default function linaria({ include, exclude, sourceMap, preprocessor, ...rest }?: RollupPluginOptions): {
    name: string;
    load(id: string): string;
    resolveId(importee: string): string | undefined;
    transform(code: string, id: string): {
        code: string;
        map: import("source-map").RawSourceMap | null | undefined;
    } | undefined;
};
export {};
